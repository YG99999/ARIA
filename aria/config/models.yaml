# Model configuration — written by the setup wizard and updated by ARIA.
# Run `aria --setup` to reconfigure, or tell ARIA to "add a model" / "update models".

# models: (populated by setup wizard)

budget:
  daily_usd_alert: 2.00
  daily_usd_hard_stop: 10.00
  monthly_usd_hard_stop: 50.00
